# data_pipeline 模組依賴套件

# 核心資料處理
pandas>=1.5.0
numpy>=1.24.0

# 資料庫
duckdb>=0.8.0  # 可選，用於高效能分析

# API 客戶端
requests>=2.28.0
shioaji>=1.3.0  # 可選，用於即時資料

# 時間處理
pytz>=2023.3
holidays>=0.34

# 日誌
loguru>=0.7.0  # 可選，用於結構化日誌

# 非同步處理
aiohttp>=3.8.0  # 可選，用於批次下載

# 開發和測試
pytest>=7.0.0  # 可選，用於單元測試
pytest-asyncio>=0.21.0  # 可選，用於非同步測試